{
  "name": "three-grid-randomizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "coverage": "vitest run --coverage",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^4.7.4",
    "vite": "^3.0.0",
    "vitest": "^0.21.0"
  },
  "peerDependencies": {
    "three": "^0.142.0"
  }
}